/** 
 * 
 */
package fr.utbm.ia54.Event

import fr.utbm.ia54.Agent.FrameAgent
import java.util.ArrayList
import java.util.UUID

/* infos :

 * - provenance
 * - timeout
 * - jump count

 * process :

 * - check timer
 * - incrase jump count
 * - check if destination myself
 * - forward multicast message
 */
 
event PathCalculation {
	
	var requestId : long
	var requestOrigin : UUID
	var provenanceDirection : int
	var timeStamp : long
	var jumpCount : int
	var path = new ArrayList<FrameAgent>()
 
	
	new (reqId : long, origin : UUID, from : int, time : long, jump : int, p : ArrayList<FrameAgent>) {
		this.requestId = reqId
		this.provenanceDirection = from
		this.requestOrigin = origin
		this.timeStamp = time
		this.jumpCount = jump
		this.path = p
	}
}
